{
  "name": "monorepo",
  "private": true,
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "local-publish": "lerna publish --no-git-tag-version --no-push"
  }
}

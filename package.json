{
  "name": "monorepo",
  "private": true,
  "devDependencies": {
    "@commitlint/cli": "^12.1.4",
    "@commitlint/config-conventional": "^12.1.4",
    "@commitlint/config-lerna-scopes": "^12.1.4",
    "commitlint": "^12.1.4",
    "husky": "^6.0.0",
    "lerna": "^4.0.0",
    "lerna-changelog": "^1.0.1"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "local-publish": "lerna publish from-package"
  },
  "husky": {
    "hooks": {
      "commit-msg": "commitlint -E HUSKY_GIT_PARAMS"
    }
  }
}
